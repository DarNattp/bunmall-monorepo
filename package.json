{
  "name": "bunmall-monorepo",
  "version": "1.0.0", 
  "workspaces": [
    "packages/*",
    "services/*",
    "frontend"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "1.3.5"
  }
}