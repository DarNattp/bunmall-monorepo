/** @type {import('next').NextConfig} */
const nextConfig = {
  // Rewrites are handled in middleware.ts to support runtime environment variables
  // async rewrites() { ... }

};

export default nextConfig;
